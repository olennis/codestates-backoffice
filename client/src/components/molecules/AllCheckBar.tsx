// @flow 
import * as React from 'react';
import styled from 'styled-components'

type Props = {
    
};
export const AllCheckBar = (props: Props) => {
    return (
        <AllCheckSection>
            나는 전체 체크박스
            
        </AllCheckSection>
    );
};

const AllCheckSection = styled.section`
    padding: 1em;
    background: red;
`